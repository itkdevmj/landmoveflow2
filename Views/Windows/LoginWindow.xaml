<Window x:Class="LMFS.Views.Windows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        mc:Ignorable="d"
        Title="토지이동흐름도 시스템 로그인" Height="450" Width="800"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoginCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Window.Resources>
        <Style x:Key="IconStyle" TargetType="Path">
            <Setter Property="Fill" Value="#cccccc"/>
            <Setter Property="Width" Value="15"/>
            <Setter Property="Height" Value="15"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="LoginTextBoxStyle" TargetType="Control">
            <Setter Property="Background" Value="#3e3e42"/>
            <Setter Property="Foreground" Value="#cccccc"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Control}">
                        <Border CornerRadius="8" Background="{TemplateBinding Background}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <ContentPresenter Content="{TemplateBinding Tag}" Margin="12,0,8,0" VerticalAlignment="Center"/>
                                <ScrollViewer x:Name="PART_ContentHost" Grid.Column="1" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF6200EE"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="10" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FF7E3FF2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="20" Background="#1e1e1e">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="0" BlurRadius="20" Color="#000000" Opacity="0.4"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" CornerRadius="20,0,0,20">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#FF6200EE" Offset="0"/>
                        <GradientStop Color="#FFB434EB" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                    <Path Data="M12,2C6.48,2 2,6.48 2,12C2,17.52 6.48,22 12,22C17.52,22 22,17.52 22,12C22,6.48 17.52,2 12,2M12,14L9,11L10.41,9.59L12,11.17L14.59,8.59L16,10L12,14Z" 
                          Fill="White" Width="80" Height="80" Stretch="Uniform"/>
                    <TextBlock Text="토지이동흐름도 시스템" FontSize="32" FontWeight="Bold" Foreground="White" Margin="0,20,0,0"/>
                </StackPanel>
            </Border>

            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="50">
                <Button x:Name="CloseButton" Content="✕" FontSize="14" Foreground="Gray" Background="Transparent" BorderThickness="0" 
                        HorizontalAlignment="Right" VerticalAlignment="Top" Cursor="Hand" Click="CloseButton_Click"
                        Margin="0,-30,0,0"/>

                <TextBlock Text="접속" FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,0,0,30"
                           HorizontalAlignment="Center"/>

                <!--<TextBlock Text="아이디" Foreground="#cccccc" Margin="0,0,0,5"/>
                <TextBox Style="{StaticResource LoginTextBoxStyle}" Margin="0,0,0,15" 
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
                <TextBlock Text="암호" Foreground="#cccccc" Margin="0,0,0,5"/>
                <PasswordBox Style="{StaticResource LoginTextBoxStyle}" Margin="0,0,0,30"
                             PasswordChanged="PasswordBox_PasswordChanged"/>-->

                <Button Content="로그인" Style="{StaticResource PrimaryButtonStyle}" Height="40"
                        Command="{Binding LoginCommand}"/>

                <!--<TextBlock HorizontalAlignment="Center" Name="StatusMessage" Foreground="AliceBlue" 
                           Margin="0,15,0,0"/>-->

                <!--<TextBlock HorizontalAlignment="Center" Margin="0,15,0,0">
                    <Hyperlink Foreground="#FF7E3FF2" TextDecorations="None">Forgot Password?</Hyperlink>
                </TextBlock>-->
            </StackPanel>
        </Grid>
    </Border>

</Window>