<Page
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:dxdiag="http://schemas.devexpress.com/winfx/2008/xaml/diagram"
      xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
      xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
      xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
      xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
      xmlns:sys="clr-namespace:System;assembly=System.Runtime"
      x:Class="LMFS.Views.Pages.LandMoveQueryPage"
      xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
      xmlns:xtraEditors="clr-namespace:DevExpress.XtraEditors;assembly=DevExpress.XtraEditors.v23.2"
      mc:Ignorable="d" 
      d:DesignHeight="320" d:DesignWidth="400"
      Title="검색필지내역"
      Loaded="LandMoveQueryPage_OnLoaded"
      SizeChanged="LandMoveQueryPage_OnSizeChanged">

    <DockPanel>

        <Grid DockPanel.Dock="Left" Width="380">
            <dxg:GridControl Grid.Row="1" 
                             x:Name="GridControl"
                             ItemsSource="{Binding GridQueryDataSource}">
                <!-- 우클릭 팝업메뉴 -->
                <dxg:GridControl.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="필지 검색" Command="{Binding QueryCommand}" />
                    </ContextMenu>
                </dxg:GridControl.ContextMenu>
                <dxg:GridControl.Columns>
                    <dxg:GridColumn FieldName="BfJibun" Header="검색필지" Width="0.8*" 
                        HorizontalHeaderContentAlignment="Center" />
                    <dxg:GridColumn FieldName="AfJibun" Header="검색일자" Width="0.8*" 
                        HorizontalHeaderContentAlignment="Center" />
                </dxg:GridControl.Columns>
            </dxg:GridControl>
        </Grid>
    </DockPanel>

</Page>